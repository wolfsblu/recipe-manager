version: "2"
sql:
  - engine: "sqlite"
    queries: "infra/sqlite/queries"
    schema: "infra/sqlite/schema.sql"
    gen:
      go:
        out: "infra/sqlite/database"
        emit_pointers_for_null_types: true